# Copy to .env and fill in. Generate the secret with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Optional: enables verification emails on register when set.
# SMTP_USER=
# SMTP_PASS=
